const sum = (a, b) => a + b;
const subtraction = (a, b) => a - b;
export default (name) => console.log(`hello ${name}`)
const text = 'it is my string'
const user= {
    name: 'medet'
}
const myBetterHalf = [
    {
        name: 'aybis'
    }
]
export {
    sum,
    subtraction,
    text,
    user,
    myBetterHalf
}